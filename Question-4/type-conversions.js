// Create a file called type-conversions.js In that file, 
// Create a variable with a type of String and takes the number 25. 
// On the next line, create another variable and assign a converted Number value of the previous variable to it. 
// Log the new variable to the console.
//  Log the type of the new variable to the console, e.g. console.log(tyepof(newVariable)). 
// Make sure to use nice and proper variable names.

// A variable with a type of String 25
let str = "25";

// Add another variable and assign a converted Number value 25 to it.
let num = Number(str);  

// Log the new variable(num) to the console. 
console.log(num);

// Log the type of the new variable(num) to the console.
console.log(typeof(num));

